# 安全政策

## 支持范围

StudyForge 仍处于 `0.x` 早期预览阶段。安全修复以当前 `main` 和最新桌面预览版为准；历史
提交、旧标签和自行修改的构建不单独维护。

## 私下报告漏洞

请不要把可利用细节、API Key、OAuth 代码、私人学习集或学生记录发到公开 Issue。

优先在仓库的 **Security** 页面使用 **Report a vulnerability** 私下提交。若该入口暂时
不可用，请先通过仓库所有者的 GitHub 主页联系维护者，只发送建立私密沟通所需的最少信息，
不要在第一条消息中附带凭据或私人数据。

报告最好包括：

- 受影响的版本或 commit；
- 操作系统与安装方式；
- 最小复现步骤和实际影响；
- 是否涉及路径访问、Provider 凭据、本地 Session、学习文件或桌面 sidecar；
- 已经采取的临时缓解措施。

## 公开 Issue 可以报告什么

不含敏感信息的崩溃、错误提示、渲染问题和普通功能缺陷可以提交公开 Issue。请先移除用户名、
本机绝对路径、Session ID、学生原话、书籍正文和模型凭据。

## 当前安全边界

- 本地优先不等于完全离线：向云端模型发消息时，当前任务所需内容会发送给所选 Provider。
- StudyForge 的 Provider 配置与普通 Pi 分离，但仍应像保护密码一样保护本机账户和应用数据目录。
- 当前 macOS 包使用 ad-hoc 签名且未经过 Apple 公证；只从项目 Release 获取，并核对发布信息。
- 用户导入的书籍和学习资料可能受版权、学校规定或个人隐私约束；不要未经授权公开分发。
