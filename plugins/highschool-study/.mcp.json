{
  "mcpServers": {
    "study-markdown": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/dist/mcp-server.js"],
      "env": {
        "STUDY_LEARNING_SET": "${CLAUDE_PROJECT_DIR}/learning-set"
      }
    }
  }
}
